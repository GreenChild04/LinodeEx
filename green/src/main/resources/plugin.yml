name: Green
version: '${project.version}'
main: com.greenchild.green.Green
api-version: 1.19
description: GreenChild's Free Alternative to Linode
commands:
  run:
    description: To run a command on the minehut server
    aliases: r
    usage: /run
  setup:
    description: To set up all needed for mining on minehut server
    aliases: su
    usage: /setup
